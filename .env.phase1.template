# =============================================================================
# Phase 1: シフトレフト機能検証用 環境変数テンプレート
# =============================================================================
# 対象シナリオ: S01-S05
# 所要時間: 10-15時間（AWS環境不要）
#
# セットアップ手順:
# 1. このファイルを .env.phase1 にコピー
# 2. 各環境変数に実際の値を設定
# 3. Phase 1シナリオ（S01-S05）を実施
#
# 詳細: docs/guides/ENVIRONMENT_VARIABLES_GUIDE.md を参照
# =============================================================================

# -----------------------------------------------------------------------------
# 1. Wiz 認証情報（必須）
# -----------------------------------------------------------------------------
# 取得方法: docs/guides/MANUAL_SETUP_GUIDE.md の「Wiz認証情報の取得」セクション参照
# Wizコンソール > Settings > Service Accounts > Create Service Account

WIZ_CLIENT_ID=your_wiz_client_id_here
WIZ_CLIENT_SECRET=your_wiz_client_secret_here

# オプション: Wizテナント名（通常は不要）
# WIZ_TENANT=your_tenant_name


# -----------------------------------------------------------------------------
# 2. GitHub 認証情報（必須）
# -----------------------------------------------------------------------------
# 取得方法: docs/guides/MANUAL_SETUP_GUIDE.md の「GitHub認証情報の取得」セクション参照
# GitHub > Settings > Developer settings > Personal access tokens > Generate new token

# Personal Access Token（repo, workflow, read:org権限が必要）
GITHUB_TOKEN=your_github_token_here

# GitHubユーザー名または組織名
GITHUB_OWNER=your_github_username_or_org


# -----------------------------------------------------------------------------
# 3. リポジトリ情報（必須）
# -----------------------------------------------------------------------------
# このWizCodeVerificationリポジトリの情報

# リポジトリ名（デフォルト: WizCodeVerification）
GITHUB_REPO=WizCodeVerification

# リポジトリのフルパス（owner/repo形式）
GITHUB_REPOSITORY=${GITHUB_OWNER}/${GITHUB_REPO}


# -----------------------------------------------------------------------------
# 4. ローカル開発環境設定（オプション）
# -----------------------------------------------------------------------------
# taskflow-app のローカル実行用（S01検証時のみ使用）

# PostgreSQL設定
DB_HOST=localhost
DB_PORT=5432
DB_NAME=taskflow_dev
DB_USER=taskflow_user
DB_PASSWORD=taskflow_password

# バックエンドAPI設定
BACKEND_PORT=3001
BACKEND_URL=http://localhost:3001

# フロントエンド設定
FRONTEND_PORT=3000
FRONTEND_URL=http://localhost:3000

# セッションシークレット（開発用）
SESSION_SECRET=dev_session_secret_change_in_production


# -----------------------------------------------------------------------------
# 5. VSCode Wiz拡張機能設定（オプション）
# -----------------------------------------------------------------------------
# S01: IDE統合シナリオで使用

# Wizワークスペース設定ファイルパス
WIZ_WORKSPACE_CONFIG=.vscode/wiz.json


# -----------------------------------------------------------------------------
# 注意事項
# -----------------------------------------------------------------------------
# ⚠️  このファイルには機密情報が含まれます
# ⚠️  .gitignoreに .env.phase1 を追加してコミット防止
# ⚠️  実際の認証情報は絶対にGitHubにプッシュしないでください
# ⚠️  検証終了後は認証情報をローテーションすることを推奨
#
# ✅  検証スクリプトで環境変数を検証:
#     Bash: ./scripts/validate-env.sh phase1
#     PowerShell: .\scripts\validate-env.ps1 phase1
# =============================================================================
