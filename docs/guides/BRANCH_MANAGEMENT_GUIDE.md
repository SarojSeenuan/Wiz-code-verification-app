# ブランチ管理ガイド

本ガイドでは、WizCode検証を繰り返し実施するためのブランチ戦略と管理方法を説明します。

> **📌 このドキュメントを読むべきタイミング**
> 検証開始前、またはブランチ戦略を理解したい時に読んでください。

> **📖 次に読むべきドキュメント**
> [Phase 1シナリオ](../phase1-shift-left/README.md) - S01から検証開始

> **🔙 ガイド一覧に戻る**
> [ガイド一覧](./README.md)

---

## 目次

1. [ブランチ戦略の概要](#ブランチ戦略の概要)
2. [ブランチ命名規則](#ブランチ命名規則)
3. [基本フロー](#基本フロー)
4. [シナリオ別ブランチ作成例](#シナリオ別ブランチ作成例)
5. [Pull Request作成と検証](#pull-request作成と検証)
6. [ブランチ削除と再作成](#ブランチ削除と再作成)
7. [保護ブランチ設定](#保護ブランチ設定)
8. [ベストプラクティス](#ベストプラクティス)

---

## ブランチ戦略の概要

### なぜブランチ管理が重要か？

WizCode検証では、以下の理由でブランチ管理が重要です：

1. **繰り返し検証**: 同じシナリオを何度も実施できる
2. **PR自動スキャン**: S02（VCS統合）でPRスキャンを検証
3. **エビデンス収集**: PRコメントやスキャン結果をキャプチャ
4. **クリーンな状態**: 検証終了後、ブランチを削除して環境をリセット

### ブランチ構成

```
main (保護ブランチ)
 ├── feature/test-s01-ide-integration    # S01検証用
 ├── feature/test-s02-vcs-integration    # S02検証用
 ├── feature/test-s03-cicd-integration   # S03検証用
 ├── feature/test-s04-iac-scanning       # S04検証用
 ├── feature/test-s05-secret-detection   # S05検証用
 ├── feature/test-s06-sbom-tracking      # S06検証用
 └── ...（S07-S11も同様）
```

---

## ブランチ命名規則

### 推奨命名規則

```
feature/test-<シナリオID>-<シナリオ名>[-任意のサフィックス]
```

**例**:
```
feature/test-s01-ide-integration
feature/test-s02-vcs-integration
feature/test-s03-cicd-integration-retry
feature/test-s04-iac-scanning-demo
```

### ブランチ種類別命名規則

| ブランチ種類 | 命名規則 | 用途 |
|------------|---------|------|
| **シナリオ検証用** | `feature/test-s<番号>-<名前>` | 各シナリオの検証実施 |
| **デモ用** | `feature/demo-<目的>` | 顧客デモ専用 |
| **バグ修正用** | `fix/<問題内容>` | ドキュメント・スクリプト修正 |
| **機能追加用** | `feature/<機能名>` | 新機能追加 |

### 避けるべきブランチ名

- ❌ `test` （曖昧すぎる）
- ❌ `s01` （シナリオIDのみ）
- ❌ `my-branch` （目的が不明）
- ❌ `temp` （一時的なブランチは削除すべき）

---

## 基本フロー

### 検証サイクル（ブランチ作成 → 検証 → マージ → 削除）

```
┌──────────────────────────────────────────────────────────┐
│                    検証サイクル                             │
└──────────────────────────────────────────────────────────┘

1️⃣ ブランチ作成
   ↓
2️⃣ 脆弱性を含むコードを追加/修正
   ↓
3️⃣ コミット & プッシュ
   ↓
4️⃣ Pull Request作成
   ↓
5️⃣ Wizスキャン実行（自動/手動）
   ↓
6️⃣ 結果確認 & エビデンス収集
   ↓
7️⃣ マージ（または却下）
   ↓
8️⃣ ブランチ削除
   ↓
9️⃣ 再検証が必要な場合、1️⃣に戻る
```

### ステップバイステップ

#### ステップ1: mainブランチを最新に更新

```bash
# mainブランチに切り替え
git checkout main

# 最新の変更を取得
git pull origin main
```

#### ステップ2: 新しいブランチを作成

```bash
# S01検証用ブランチの作成例
git checkout -b feature/test-s01-ide-integration

# 確認
git branch
```

#### ステップ3: 脆弱性を含むコードを追加/修正

シナリオに応じて、意図的に脆弱なコードを追加します。

**例（S01: IDE統合）**:
```javascript
// taskflow-app/backend/src/routes/tasks.js
// 脆弱性: SQL注入
app.get('/api/tasks/search', (req, res) => {
    const query = req.query.q;
    // ❌ 脆弱: ユーザー入力を直接SQL

に埋め込み
    const sql = `SELECT * FROM tasks WHERE title LIKE '%${query}%'`;
    db.query(sql, (err, results) => {
        res.json(results);
    });
});
```

#### ステップ4: 変更をコミット & プッシュ

```bash
# 変更を確認
git status

# ステージング
git add .

# コミット
git commit -m "S01検証: SQL注入脆弱性を追加"

# リモートにプッシュ
git push -u origin feature/test-s01-ide-integration
```

#### ステップ5: Pull Request作成

```bash
# GitHub CLIを使用（推奨）
gh pr create \
  --title "S01: IDE統合検証 - SQL注入脆弱性" \
  --body "S01シナリオ検証用PRです。意図的にSQL注入脆弱性を含みます。" \
  --base main \
  --head feature/test-s01-ide-integration

# または、GitHub Web UIから作成
```

#### ステップ6: Wizスキャン実行

- **S02（VCS統合）**: PR作成時に自動スキャン
- **S03（CI/CD統合）**: GitHub Actionsで自動スキャン
- **その他**: 手動でWiz CLIスキャン

#### ステップ7: 結果確認 & エビデンス収集

- PRコメントでスキャン結果を確認
- Wizコンソールで詳細確認
- スクリーンショットを撮影（エビデンス）

#### ステップ8: マージまたは却下

```bash
# マージ（GitHub CLI）
gh pr merge --merge

# または、GitHub Web UIからマージ
```

#### ステップ9: ブランチ削除

```bash
# ローカルブランチを削除
git checkout main
git branch -D feature/test-s01-ide-integration

# リモートブランチを削除
git push origin --delete feature/test-s01-ide-integration
```

---

## シナリオ別ブランチ作成例

### S01: IDE統合

```bash
git checkout main
git pull origin main
git checkout -b feature/test-s01-ide-integration

# 脆弱性を追加（SQLインジェクション、XSSなど）
# コミット & プッシュ

git add .
git commit -m "S01検証: SQL注入とXSS脆弱性を追加"
git push -u origin feature/test-s01-ide-integration
```

### S02: VCS統合

```bash
git checkout main
git pull origin main
git checkout -b feature/test-s02-vcs-integration

# 脆弱性を追加
# Pull Request作成（Wiz GitHub Appが自動スキャン）

gh pr create \
  --title "S02: VCS統合検証 - 認証バイパス脆弱性" \
  --body "S02シナリオ検証用PR。Wiz GitHub Appによる自動スキャンを確認します。"
```

### S03: CI/CD統合

```bash
git checkout main
git pull origin main
git checkout -b feature/test-s03-cicd-integration

# .github/workflows/S03-wiz-full-scan.ymlが既に存在
# コミット & プッシュで自動的にGitHub Actionsが実行される

git add .
git commit -m "S03検証: CI/CDパイプライン統合"
git push -u origin feature/test-s03-cicd-integration
```

### S04: IaCスキャン

```bash
git checkout main
git pull origin main
git checkout -b feature/test-s04-iac-scanning

# terraform/modules/rds/main.tf に脆弱性を追加
# 例: publicly_accessible = true

git add terraform/
git commit -m "S04検証: RDSパブリックアクセス有効化"
git push -u origin feature/test-s04-iac-scanning
```

### S05: シークレット検出

```bash
git checkout main
git pull origin main
git checkout -b feature/test-s05-secret-detection

# ハードコードされたシークレットを追加
# 例: AWS_ACCESS_KEY_ID="AKIAIOSFODNN7EXAMPLE"

git add .
git commit -m "S05検証: ハードコードされたAWS認証情報"
git push -u origin feature/test-s05-secret-detection
```

---

## Pull Request作成と検証

### GitHub CLIでのPR作成

```bash
# 基本的なPR作成
gh pr create

# 詳細を指定してPR作成
gh pr create \
  --title "S01: IDE統合検証" \
  --body "$(cat <<EOF
## 検証目的
S01シナリオの検証用PRです。

## 追加された脆弱性
- SQL注入脆弱性（tasks.js）
- XSS脆弱性（api.js）

## 期待される結果
- Wiz IDEプラグインで検出される
- Wiz GitHub Appでスキャンされる
- PRコメントに結果が表示される
EOF
)" \
  --base main \
  --head feature/test-s01-ide-integration
```

### GitHub Web UIでのPR作成

1. GitHubリポジトリページを開く
2. **Pull requests** → **New pull request**
3. **base**: `main`、**compare**: `feature/test-s01-ide-integration`
4. **Create pull request**
5. タイトル・説明を入力して作成

### PRでのWizスキャン確認

#### S02（VCS統合）での確認

- PR作成後、数分以内にWiz GitHub Appがスキャン
- PRコメントに結果が表示される
- 「**Files changed**」タブでインラインコメントを確認

#### S03（CI/CD統合）での確認

- PRまたはプッシュ時にGitHub Actionsが起動
- **Actions**タブでワークフロー実行状況を確認
- スキャン完了後、結果がジョブログに表示

---

## ブランチ削除と再作成

### なぜブランチを削除するのか？

1. **クリーンな状態**: 次回検証時に混乱を避ける
2. **リポジトリの整理**: 不要なブランチを削除してリポジトリを整理
3. **再検証の準備**: 同じシナリオを再度検証する際、新しいブランチで開始

### ブランチ削除手順

#### ローカルブランチの削除

```bash
# mainブランチに切り替え
git checkout main

# ブランチを削除
git branch -D feature/test-s01-ide-integration

# 確認
git branch
```

#### リモートブランチの削除

```bash
# リモートブランチを削除
git push origin --delete feature/test-s01-ide-integration

# 確認
git branch -r
```

#### GitHub CLIでの一括削除

```bash
# マージ済みブランチを一括削除
gh pr list --state merged --json headRefName --jq '.[].headRefName' | xargs -I {} git push origin --delete {}
```

### 再検証のためのブランチ再作成

```bash
# mainを最新に更新
git checkout main
git pull origin main

# 同じ名前でブランチを再作成
git checkout -b feature/test-s01-ide-integration

# 脆弱性を再度追加
# コミット & プッシュ
```

---

## 保護ブランチ設定

### mainブランチの保護

mainブランチを保護して、誤った直接プッシュを防ぎます。

#### GitHubでの設定手順

1. GitHubリポジトリ → **Settings** → **Branches**
2. **Add rule** または既存ルールを編集
3. **Branch name pattern**: `main`
4. 以下を有効化:
   - ✅ **Require a pull request before merging**
   - ✅ **Require approvals** （1人以上の承認）
   - ✅ **Require status checks to pass before merging**
   - ✅ **Require conversation resolution before merging**
   - ✅ **Do not allow bypassing the above settings**
5. **Create** または **Save changes**

### 推奨設定

| 設定項目 | 推奨値 | 理由 |
|---------|--------|------|
| Require pull request | ✅ 有効 | 直接プッシュを防止 |
| Required approvals | 1人 | レビュープロセスを担保 |
| Require status checks | ✅ 有効 | CI/CD成功を確認 |
| Required status checks | `wiz-scan` | Wizスキャン成功を必須化 |

---

## ベストプラクティス

### 1. ブランチ作成前にmainを更新

```bash
# 常に最新のmainから開始
git checkout main
git pull origin main
git checkout -b feature/test-s01-ide-integration
```

### 2. コミットメッセージは明確に

```bash
# ❌ 悪い例
git commit -m "update"

# ✅ 良い例
git commit -m "S01検証: SQL注入脆弱性を追加（tasks.js）"
```

### 3. PRは小さく保つ

- 1つのPRに1つのシナリオ
- 複数のシナリオを混在させない

### 4. PR説明を充実させる

```markdown
## 検証目的
S01シナリオの検証用PRです。

## 追加された脆弱性
- SQL注入脆弱性（src/routes/tasks.js:45）
- XSS脆弱性（lib/api.js:102）

## 期待される結果
- Wiz IDEプラグインで2件の脆弱性を検出
- Wiz GitHub Appでスキャン成功

## スクリーンショット
（エビデンス用スクリーンショットを添付）
```

### 5. エビデンスをPRに記録

- スキャン結果のスクリーンショットをPRコメントに添付
- Wizコンソールへのリンクを記載
- 検出された脆弱性の詳細を記録

### 6. 検証完了後は速やかに削除

```bash
# マージ後、すぐに削除
gh pr merge --merge
git branch -D feature/test-s01-ide-integration
git push origin --delete feature/test-s01-ide-integration
```

### 7. ブランチ一覧を定期的に確認

```bash
# ローカルブランチ一覧
git branch

# リモートブランチ一覧
git branch -r

# 不要なブランチを削除
git branch -D <ブランチ名>
git push origin --delete <ブランチ名>
```

---

## クイックリファレンス

### よく使うGitコマンド

```bash
# ブランチ作成と切り替え
git checkout -b feature/test-s01-ide-integration

# 変更をステージング
git add .

# コミット
git commit -m "S01検証: SQL注入脆弱性を追加"

# プッシュ
git push -u origin feature/test-s01-ide-integration

# PR作成（GitHub CLI）
gh pr create

# mainに切り替え
git checkout main

# ブランチ削除（ローカル）
git branch -D feature/test-s01-ide-integration

# ブランチ削除（リモート）
git push origin --delete feature/test-s01-ide-integration
```

### よく使うGitHub CLIコマンド

```bash
# PR作成
gh pr create

# PR一覧表示
gh pr list

# PRの詳細表示
gh pr view 123

# PRマージ
gh pr merge 123 --merge

# PRクローズ
gh pr close 123
```

---

## 次のステップ

ブランチ戦略を理解したら、以下のドキュメントに進んでください：

1. **[Phase 1シナリオ](../phase1-shift-left/README.md)** - S01から検証開始
2. **[EVIDENCE_COLLECTION_GUIDE.md](./EVIDENCE_COLLECTION_GUIDE.md)** - エビデンス収集方法
3. **[TROUBLESHOOTING_GUIDE.md](./TROUBLESHOOTING_GUIDE.md)** - 問題発生時の対処

---

## 参考資料

- [Git公式ドキュメント](https://git-scm.com/doc)
- [GitHub Flow](https://docs.github.com/en/get-started/quickstart/github-flow)
- [GitHub CLI](https://cli.github.com/)
- [保護ブランチ](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)

---

**🔙 [ガイド一覧に戻る](./README.md)**
